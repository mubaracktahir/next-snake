export const GRID_SIZE = 20; // Number of cells in each row/column
export const CELL_SIZE = 20; // Size of each cell in pixels
export const INITIAL_SNAKE_POSITION = [{ x: Math.floor(GRID_SIZE / 2), y: Math.floor(GRID_SIZE / 2) }];
export const INITIAL_DIRECTION = 'RIGHT';
export const GAME_SPEED_MS = 150; // Snake moves every 150ms
